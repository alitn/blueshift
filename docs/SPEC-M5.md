# SPEC-M5 — L4 flywheel (summary)

To be fully specified after M4. Scope: per-clip performance ingestion (views/engagement per
platform); network-specific ranker trained on accept + performance history; reporting.
Additive only.
