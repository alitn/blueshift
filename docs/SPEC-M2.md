# SPEC-M2 — Pilot hardening (summary)

To be fully specified after M1 feedback. Scope: approvals inbox with the editor/approver
roles split (`allow_self_approval` flips off per org); batch-approve; prompt search over
segments (pgvector semantic + pg_trgm); cover-frame picker; Shorts + X presets (config rows);
deferred polish from M1 pilot feedback. All additive — no schema rewrites, no structural UI
changes.
