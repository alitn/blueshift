# SPEC-M3 — L2 audio intelligence (summary)

To be fully specified after M2. Scope: audio event tags (applause, laughter, music, tone
shifts) as additive signal tables; candidate re-rank flag using audio events; accept-rate
report comparing ranker versions against editor decisions (from the audit trail). Additive
only.
